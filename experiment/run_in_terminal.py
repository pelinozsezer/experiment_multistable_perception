import sysdef function(x):    if x==1:        print('it works for 1')    else:        print('it works for else')                # # Open the text file for reading# with open('data.txt', 'r') as file:#     # Read the lines from the file and store them in a list#     lines = file.readlines()# # Process the lines, e.g., convert them to integers# values = [int(line.strip()) for line in lines]# # Print the imported values# for value in values:#     print(value)